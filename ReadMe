##This a project for the course of Problem Solving

To run the code, you need to run the command like this :

javac hashcode.java
java hashcode nameOftheFile

To create all the instances run the command :

javac Instance.java
java Instance
